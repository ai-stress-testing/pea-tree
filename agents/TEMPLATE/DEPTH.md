# Role Name — Depth Pack

Optional L1 for this role. Loaded ONLY on a depth trigger (novelty off-charter
/ high-stakes call / FAIL-retry) — see `docs/depth-packs.md`. Not resident;
never duplicate the L0 charter here. Keep to ~50 lines. Exemplars, not
adjectives.

## Exemplars

2–3 concrete, repo-relevant worked cases of this role done right. For each:
the situation, the move, and the *tell* that separates the right move from the
plausible-but-wrong one. Show the reasoning, not a bio.

- **<situation>** — <what the role did, and why that was the correct call>.
  Tell: <the cue a weaker pass would have missed>.

## Failure-mode playbook

The specific ways this role goes wrong — the negative space the exemplars
imply. Each with its catch.

- **<failure mode>** → <how to catch it before it ships>.

## Priors & voice

Standing assumptions and tone in a few lines. Still invariants and examples,
not vibe copy.

- <a prior this role always carries>.
- Voice: <one phrase>.
